# Synthetic test fixtures

**Fake data only.** Nothing here is derived from real biobank records.
Sample IDs are fabricated (e.g. `TEST_001`, `TEST_002`) and deliberately do not
match MSM ID patterns.
